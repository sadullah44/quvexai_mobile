import 'package:flutter/material.dart';

class QrTab extends StatelessWidget {
  const QrTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('QR Kod Tarama Sekmesi'));
  }
}
